# Model drag diagnosis

Hypotheses: (1) new opaque PreviewNotice covers/intercepts gizmo, (2) copper bounds change camera/handle position, (3) extracted ModelMesh changes TransformControls attachment. Screenshot puts new notice over the fixed gizmo target; source comparison shows model handlers/transforms retained. Isolated existing test run port3080; temporary cloned app/e2e/renderer-model-drag.probe.spec.ts will record elementFromPoint and hide only the notice before replaying unchanged drag and persistence assertions. Delete probe after replay; retain code and logs in evidence.

Confirmed root cause: isolated existing test reproduces unchanged [8.89,16.5,-8.2] offset; the browser's elementFromPoint at the unchanged gizmo coordinates returns the new P[role=status] warning. Hiding only that warning makes the complete unchanged drag/greater-than3mm/undo/export assertions pass (probe.log). Thus no camera/control/coordinate regression is needed to explain failure. Probe screenshots reveal the model previously hidden behind the opaque notice.

Minimal production fix: replace the always-expanded warning overlay with a native collapsed details disclosure, labelled Preview limitations. All diagnostic text stays available on expansion; collapsed content no longer hides or intercepts the gizmo. No changes to camera, ModelMesh, transformations or original e2e assertions. New FootprintCanvas.test.tsx fails before disclosure (notice-red.txt), then passes; all11 focused renderer tests pass (notice-green.txt). Touched files formatted and ESLint clean. Temporary app/e2e/renderer-model-drag.probe.spec.ts removed; its source remains intentionally in evidence as probe.spec.txt. Root will run precommit/build and replay original test against the production fix, then recapture affected visual surfaces.
