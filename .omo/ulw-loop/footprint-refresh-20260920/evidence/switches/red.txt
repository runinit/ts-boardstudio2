180 native engine-generated switch cases; 18 failures
socket contacts r0: switch_choc_v1_v2 {"side":"B","reversible":false,"hotswap_pads_same_side":true,"include_plated_holes":false} B.Cu
+ actual - expected

+ Set(1) {
- Set(2) {
    'FROM',
-   'TO'
  }

route ownership r0: mixed track junction

'TO' !== 'FROM'

mount net flags r0: Expected values to be strictly equal:

'' !== 'RIGHT'

mount net flags r0: Expected values to be strictly equal:

'RIGHT' !== ''

outer width r0: The expression evaluated to a falsy value:

  assert.ok(Math.abs(width - (side === 'F' ? 1.6 : 2.0)) < 1e-8)

outer width r0: The expression evaluated to a falsy value:

  assert.ok(Math.abs(width - (side === 'F' ? 1.6 : 2.0)) < 1e-8)

socket contacts r37: switch_choc_v1_v2 {"side":"B","reversible":false,"hotswap_pads_same_side":true,"include_plated_holes":false} B.Cu
+ actual - expected

+ Set(1) {
- Set(2) {
    'FROM',
-   'TO'
  }

route ownership r37: mixed track junction

'TO' !== 'FROM'

mount net flags r37: Expected values to be strictly equal:

'' !== 'RIGHT'

mount net flags r37: Expected values to be strictly equal:

'RIGHT' !== ''

outer width r37: The expression evaluated to a falsy value:

  assert.ok(Math.abs(width - (side === 'F' ? 1.6 : 2.0)) < 1e-8)

outer width r37: The expression evaluated to a falsy value:

  assert.ok(Math.abs(width - (side === 'F' ? 1.6 : 2.0)) < 1e-8)

socket contacts r90: switch_choc_v1_v2 {"side":"B","reversible":false,"hotswap_pads_same_side":true,"include_plated_holes":false} B.Cu
+ actual - expected

+ Set(1) {
- Set(2) {
    'FROM',
-   'TO'
  }

route ownership r90: mixed track junction

'TO' !== 'FROM'

mount net flags r90: Expected values to be strictly equal:

'' !== 'RIGHT'

mount net flags r90: Expected values to be strictly equal:

'RIGHT' !== ''

outer width r90: The expression evaluated to a falsy value:

  assert.ok(Math.abs(width - (side === 'F' ? 1.6 : 2.0)) < 1e-8)

outer width r90: The expression evaluated to a falsy value:

  assert.ok(Math.abs(width - (side === 'F' ? 1.6 : 2.0)) < 1e-8)

node:internal/modules/run_main:107
    triggerUncaughtException(
    ^

AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:

18 !== 0

    at file:///home/chris/projects/ts-boardstudio2/footprints/scripts/refreshSwitches.test.mjs:84:8 {
  generatedMessage: true,
  code: 'ERR_ASSERTION',
  actual: 18,
  expected: 0,
  operator: 'strictEqual',
  diff: 'simple'
}

Node.js v24.14.0
