# Footprint refresh

Use latest source GitHub footprints as reference; fix all issues identified during research; make the footprint library display actual geometry for side, reversible and supported parameter values; expose those parameters for GUI preview.

Preserve namespaces, storage identities, licenses and BHK independence. Do not import the entire external catalogue. Distinguish proven generator defects from unresolved physical manufacturing claims. Implement and validate software corrections, document any physically unverified limits. Commit verified increments; no publishing or deployment.

HEAVY delivery: footprint source, engine parameter handling, worker generation and React preview integration span domains.

Discovery in progress: latest upstream comparison, exhaustive research issue reconciliation, current preview/parameter data flow. No production edits before failing-first evidence.

## Registered task objective

Refresh footprints using the latest source GitHub footprints as reference; fix all issues identified during research; ensure the footprint library displays the correct footprint for side, reversible, and all supported parameter settings; expose those parameters in the footprint library for GUI preview.
