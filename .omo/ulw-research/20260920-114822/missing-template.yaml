schema: ergogen/v1
layout:
  objects:
    c:
      kind: component
      pcb: main
      footprints:
        switch:
          what: mx
          params:
            from: "{{missing}}"
            to: OK
designs:
  regions:
    board:
      shape:
        size:
          - 80
          - 60
  profiles:
    board:
      from: regions.board
pcbs:
  main:
    profile: profiles.board
