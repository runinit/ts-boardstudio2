# wave-3-freeze_handoff

outlineOnly key JSON.stringify([config,assets]) sees prepared source. freezeOutlines writes snapshots+auto=false; following parse gets different key and rejects one-shot staged geometry. Snapshot path may be cheap; no measured duplicate-cost claim. Proposed private exact-freeze handoff token requires proof before performance claim; never ignore snapshots broadly. Dependencies include scene geometry/matrices/selectors/recipeparameters/assetbytes/kernelversion; export options only if caching serialized output. EXPAND probe routed L.
