# Integration result

Worker injection order staged core -> catalogue -> runtime project injections. Library aliases immutable library/<slug>_<uuid>. UI derives primitive params from module.params; structured values hidden. Native layout shallow-merges params. No explicit capability registry for side/reverse/reversible conventions. Sources: app/src/workers/ergogen.worker.ts:10; app/src/utils/footprintEngine.ts:28; engine/src/native/layout.js:217; footprints/src/defaultModels.mjs.

## EXPAND
- LEAD: literal numeric net ids in bodies — WHY: dynamic net indexing cannot repair literal metadata — ANGLE: scan and compare final native outputs.
