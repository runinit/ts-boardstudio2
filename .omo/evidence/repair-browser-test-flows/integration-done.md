# Integration DoneClaim

Task 3 verification and authorized local commits completed. Full suite is not green; no unrelated fixes were made.

- Native commit: e39d982, Align native draft tests with default project creation.
- BHK commit: 9321562, Use current BHK row editing controls in browser tests.
- Tested base SHA: 6a130dcf655dee60414e1f43fe76134d84ea20e3 plus exact three-file diff in integration-start.txt and precommit/before.diff. Formatting made no source changes. Commits contain that same tested code. Evidence-only commit follows this receipt.
- Node 24.14.0 and pnpm 11.26.0; PATH begins /home/chris/.nvm/versions/node/v24.14.0/bin. No rebuild after tests-only edits. Fresh-build receipt build.log/build.exit; same dist index hash in integration-start.txt and integration-cleanup.txt; no production diff.

## Scenarios, invocation, binary observable and artifacts

1. Helper consumers: `PLAYWRIGHT_PORT=4184 PLAYWRIGHT_HTML_OPEN=never timeout 1800 pnpm --dir app exec playwright test e2e/routing.spec.ts e2e/responsive.spec.ts e2e/icons.spec.ts e2e/studio-settings-and-svg.spec.ts --workers=1 --output=../.omo/evidence/repair-browser-test-flows/consumers --trace=retain-on-failure --reporter=list`. Actual exit 1, 11 passed / 2 failed. Artifacts: consumers.log, consumers.exit, consumers/. All six routing cases pass, including unchanged length=2 and distinct-ID count=2 checks. Helper only asserts readiness; creates no projects. Later mobile Code and Offline App failures are unrelated; see baseline probe.
2. Complete browser suite: `PLAYWRIGHT_PORT=4184 PLAYWRIGHT_HTML_OPEN=never timeout 3600 pnpm --dir app exec playwright test --workers=2 --output=../.omo/evidence/repair-browser-test-flows/full --trace=retain-on-failure --reporter=list`. Actual exit 1: 172 passed, 20 failed, 1 existing skipped; 193 total, 24.2m. Artifacts: full.log, full.exit, full/. All three target cases and duplicate-project routing pass. Each failure's exact name/error/path is in failure-classification.md and failure-details.json. Existing skipped GitHub URL/footprints test is not claimed passing. Retries=0, no max-failure cutoff.
3. Baseline consumer attribution: task-owned Vite preview on 4184, `timeout 90 node .omo/evidence/repair-browser-test-flows/baseline-probe/probe.cjs`. Actual exit 0 means both expected downstream failures reproduced. Baseline helper text was read from the recorded base before commits; changed createDraft is never invoked. Direct /new navigation yields /boardstudio/ and 20 keys. Baseline openInspector/openCode finds zero accessible Code buttons at 375px; original Settings assertion finds zero Offline App texts. Artifacts: baseline-probe/probe.log, probe.exit, results.json, base-studio.txt, screenshots and traces. This is a focused baseline-source probe against unchanged production, not a second full baseline checkout run.
4. Required precommit: `VITEST_MAX_FORKS=2 VITEST_MIN_FORKS=1 VITEST_MAX_THREADS=2 VITEST_MIN_THREADS=1 timeout 1800 pnpm precommit`. Actual exit 0; format, lint, Knip, app typecheck, footprints and 140 unit files / 922 tests pass. Artifacts: precommit.log, precommit.exit, precommit/before-status.txt and before.diff. Existing warning output retained. No formatter changes.
5. Extra strict e2e diagnostic probe: `pnpm --dir app exec tsc --noEmit --strict --skipLibCheck --target ES2022 --module ESNext --moduleResolution Bundler --esModuleInterop e2e/app.spec.ts e2e/bhk-matrix.spec.ts e2e/utils/studio.ts`. Exit 2: existing openInspector getByText selector option TS2353 at studio.ts:95. That function is unchanged from base-studio.txt. Artifact: precommit/scoped-tsc.log. This is a disclosed preexisting limitation, not a clean whole-e2e typecheck claim. Required app typecheck passes and scoped ESLint receipts exist; no LSP roundtrip is claimed.

## Scope and cleanup

Only three assigned test files changed outside task evidence. Six tracked screenshots generated by unchanged full-suite specs were copied into full-generated-screenshots/ then restored to HEAD; they were clean in initial status. No other checkout was touched. No source instrumentation, timeout increases, retries, forced clicks or new skips were added. Port 4184 is free and owned browser/preview processes ended; integration-cleanup.txt captures this. Runtime environments were process-local.

Raw trace bulk remains local intentionally and is listed with SHA256 and byte size in artifact-manifest.json. Portable evidence commits include text receipts/logs, the three target RED traces and required target screenshots. Full-suite raw traces are not all committed; handoff is intentionally not an entirely clean worktree. Root-managed .omo state and later review reports remain uncommitted. No push, merge or PR occurred. Final reviews and user handoff belong to the root agent.
