# Code review

Final verdict: APPROVE (code-review lane; corrective delta reviewed).

Current final binding after the component-selection harness correction: base HEAD `347229abc1561a44a2dc6cc0cee50969b49b267e`, actual staged tree `348e0e5f520a45268ba80a01f90627f8a0f575d8`, current `frozen-final.patch` SHA256 `2629ad6baca3a38072a3edb2777416d82a75e2a16d8a02943047db0ad70623ec`. Independently verified tree and digest. The entire delta from the previously approved tree is the selection route in `app/e2e/utils/studioPerformance.ts`: Select Objects, then Enter on the owned diode's canvas group. Inspected `StudioCanvas.tsx`'s focusable `data-object` group and real Enter handler (`pick` → `selectTargets` → `onSelect`). The prior free-object list excluded clustered owned components. X=1 source assertion, matching worker completion, exact undo/redo, reload equality, and screenshot remain unchanged. No production change or weakened assertion. Root reports focused browser PASS plus formatter/lint PASS; browser evidence ownership remains with QA. Code approval carries forward to this exact staged tree.

Previous binding after the cell-assertion correction: base HEAD `347229abc1561a44a2dc6cc0cee50969b49b267e`, actual staged tree `1fdba0718f9c4e9cdb4f1b739969998459d80198`, retained `frozen-cell-assertion.patch` SHA256 `6744f97826c0bbc3b29f778cff39ab674d5a86dc5f08281afd42a31853824eb6`. Independently checked the tree, patch digest, and complete delta against the previously approved staged tree. Exactly two source lookup IDs in `app/e2e/studio-performance.spec.ts` now use `fingers_c2_r2`, matching the actual selected column and clicked removal button. Key counts and restored `['c2', 'r2']` cell assertions are preserved. No production changes; production approval carries forward. Corrected full-flow browser execution remains the separate QA lane's responsibility, and is not claimed passed by this source review.

Previous production-delta binding: base HEAD `347229abc1561a44a2dc6cc0cee50969b49b267e`, actual staged tree `6b20edc487ea64844e137cfae164e7fca1b36d07`, retained `frozen-led.patch` SHA256 `bf8e6d411dd7cc6fd101580a7764a6ca2290c6f830bbfe0e2b20dcfd345b4d6d`. Independently verified tree and digest. These are uncommitted staged-content approvals, not HEAD-content approvals.

The P1 below is resolved: `applyNewKeyDefaults` now batches only consecutive equal recipes and flushes pending groups before fallback defaults, retaining insertion order across A/B/A and mixed assembly/non-assembly paths. `assemblyInterleaving.test.ts` independently pins historical P4 inputs and compares the batch result. `mutations/led-interleaving/final-green.log` records 4 files / 17 tests passed on the final delta; final typecheck/lint logs have no diagnostics and executor reports both exit 0. No remaining material code findings. Final build/browser and snap classification belong to the separate verification lane and were pending at approval; this approval does not represent an all-green integration check.

## First-pass history: REQUEST_CHANGES

Reviewed base HEAD `347229abc1561a44a2dc6cc0cee50969b49b267e`, actual staged tree `4f7844ec52edbbf680eaf922237f39e24126f9ff`, frozen patch SHA256 `a0edd1e865fb509301e9b84687c3731da6a2f6c139746e2395dd36109b0ea3bb`. Independently checked tree and patch digest. This is staged content, not a claim that HEAD contains the implementation. Git author identity is unset; no commit was created. Unrelated AGENTS changes excluded.

## Material finding

- **P1 — preserve insertion order across different assembly recipes.** `app/src/utils/keyOptions.ts:405–413` groups all equal setups globally. With LED-enabled new keys whose column recipes are A/B/A, assembly application appends LED objects in c1/c3/c2 order instead of historical c1/c2/c3 order. `syncLedChains` consumes insertion order, so this changes generated electrical connections, not merely YAML formatting. The new row's actual P4 inputs are `[fingers_c3_r1_led_out, fingers_c3_r2_led_out, fingers_c1_r2_led_out]`; sequential defaults produce `[fingers_c3_r1_led_out, fingers_c1_r2_led_out, fingers_c2_r2_led_out]`. Evidence: `mutations/led-interleaving/red.log`, failing `assemblyInterleaving.test.ts` comparison against historical public-operation composition. Preserve sequential recipe order while batching compatible consecutive keys, and retain this regression.

## Other reviewed areas

No additional material defect found in source-range append/replacement/removal; retained comments/CRLF, missing fields, alias validation, lock/review boundaries, child ownership and collision handling have targeted semantic coverage. Offset memoization clones stored and returned models and remains local to a parse. Staged geometry is cloned before exposure, keyed by complete config/assets plus prepared scene identity, and consumed once. Repair-ray prototype fallback is limited to repair subdivisions; successful direct offsets remain unchanged. Exact contour/SVG/DXF pins and staged/full analysis parity cover these changes. Outline recipe reuse excludes precisely the current board's ownership metadata path; shared, authored, and other-board references remain protected.

Reviewed source/tests and existing verification artifacts without running CPU-heavy tests or browser jobs. Final precommit log ends with 138 app test files / 917 tests passed before the new interleaving regression. Earlier integration run has 335 engine tests and 913 app tests passed, but its browser run is not green (163 passed, 29 failed, one skipped). Three ambiguous long cases subsequently passed serially. The snap case and final isolated browser evidence remained pending at this review; existing causal classifications do not authorize claiming the entire check passed. The reproduced electrical regression alone determines this verdict.
