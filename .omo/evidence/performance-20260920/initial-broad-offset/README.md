# Superseded measurements

These measurements used the initial all-direct-offset ray change, which later
failed the full engine rounding regression. They are retained for provenance,
not final acceptance. Current top-level matched results are regenerated after
limiting the optimization to contractions. See `../offset-regression.md`.
