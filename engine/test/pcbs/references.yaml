points.zones.matrix:
  columns.one:
  rows.only:
pcbs:
  shown:
    template: kicad5
    references: true
    footprints:
      - what: references_test
  hidden:
    template: kicad5
    footprints:
      - what: references_test