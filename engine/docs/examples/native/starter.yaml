schema: ergogen/v1
meta:
  name: Starter
parts:
  mx:
    revision: "1"
    envelopes:
      pcb:
        size:
          - 18
          - 18
      plate:
        size:
          - 14
          - 14
      body:
        size:
          - 14
          - 14
        height:
          - 0
          - 11.6
    footprints:
      switch:
        what: mx
        params:
          from: "{{column_net}}"
          to: "{{row_net}}"
layout:
  objects:
    key:
      kind: key
      part: mx
designs:
  regions:
    keys:
      select:
        kind: key
      envelope: pcb
  profiles:
    board:
      from: regions.keys
      clearance: 2
