# Bundled BHK example

`bhk/` preserves the bundled example and its footprint namespace. The separate
BHK design checkout remains independent.

The engine and model library are workspace packages in `../../engine/` and
`../../footprints/`. Previous generator archives remain available in Git history.
